const Xlsx = require('xlsx');
const fs = require('fs');
function readExcel(filePath){
// var workBook = Xlsx.read(filePath);
// var worksheet = workbook.Sheets['financial'];
// var data = Xlsx.utils.sheet_to_json(worksheet);
console.log('data');
}